this app allows the user to place a ship on a trackable plane serface. the posablie uses for this are for a AR game of "battle ship".

as it stands now however this only allows one type of ship and can only be placed in a single orientation on the surface.